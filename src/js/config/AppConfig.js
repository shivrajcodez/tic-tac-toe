/**
 * AppConfig
 * Central configuration constants.
 */
export const AppConfig = Object.freeze({
  COMPUTER_MOVE_DELAY_MS: 650, // Delay before AI plays (feels more natural)
  DEFAULT_DIFFICULTY: 'hard',
  DEFAULT_MODE: 'pvc',
});
